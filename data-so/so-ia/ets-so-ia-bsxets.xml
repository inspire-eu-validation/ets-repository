<?xml version="1.0" encoding="utf-8"?>
<ExecutableTestSuite xmlns="http://www.interactive-instruments.de/etf/2.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" id="EID28051c99-ff98-480c-b8c1-b502333cc3cd" xsi:schemaLocation="http://www.interactive-instruments.de/etf/2.0 https://resources.etf-validator.net/schema/v2/val/ignoreRef.xsd">
	<itemHash>bQ==</itemHash>
	<remoteResource>https://github.com/inspire-eu-validation/ets-repository/tree/master/data-so/so-ia</remoteResource>
	<localPath>/auto</localPath>
	<label>Conformance class: Information accessibility, Soil</label>
	<description><![CDATA[This test suite examines a data set against the themespecific requirements related to the accessibility of resources referenced by the data.<br/><br/>
<b>This is a draft version. It has limitations and is expected to contain errors.</b> Please report any issues or problems <a href="https://github.com/INSPIRE-MIF/helpdesk-validator" target="_blank">in GitHub</a>.<br/><br/>
Known limitations are documented in the description of the applicable test case or test assertion. There is a general limitation in all assertions that extensions in additional application schemas are only supported, if the unqualified name of the feature type in the extension is the same as the name of the feature type in the INSPIRE application schema.<br/><br/>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia" target="_blank">Conformance Class 'Information accessibility, Soil'</a><br/><br/>
Pre-requisite conformance classes:
<ul><li><a href="http://inspire.ec.europa.eu/id/ats/data/master/information-accessibility" target="_blank">Conformance Class 'Information accessibility'</a></li>
</ul>]]></description>
	<reference>../../inspire-noggeo-bsxets.xq</reference>
	<version>0.1.0</version>
	<author>Consortium Bilbomatica, Guadaltel y Geograma</author>
	<creationDate>2021-05-07T12:15:00Z</creationDate>
	<lastEditor>Consortium Bilbomatica, Guadaltel y Geograma</lastEditor>
	<lastUpdateDate>2021-05-07T12:15:00Z</lastUpdateDate>
	<tags>
		<tag ref="EID57914240-3457-4c8f-96a0-347440b3d739"/>
	</tags>
	<testDriver ref="EID4dddc9e2-1b21-40b7-af70-6a2d156ad130"/>
	<translationTemplateBundle ref="EID70a263c0-0ad7-42f2-9d4d-0d8a4ca71b52"/>
	<ParameterList name="ETF Standard Parameters for XML test objects">
		<parameter name="files_to_test" required="true">
			<defaultValue>.*</defaultValue>
			<description ref="TR.filesToTest"/>
			<allowedValues>.*</allowedValues>
			<type>string</type>
		</parameter>
		<parameter name="tests_to_execute" required="false">
			<defaultValue>.*</defaultValue>
			<description ref="TR.testsToExecute"/>
			<allowedValues>.*</allowedValues>
			<type>string</type>
		</parameter>
	</ParameterList>
	<supportedTestObjectTypes>
		<testObjectType ref="EIDe1d4a306-7a78-4a3b-ae2d-cf5f0810853e"/>
	</supportedTestObjectTypes>
	<dependencies>
		<executableTestSuite ref="EID499937ea-0590-42d2-bd7a-1cafff35ecdb"/>
	</dependencies>
	<testModules>
		<TestModule id="EID4a718a43-3738-4a57-aa9e-a1dbbda89bd9">
			<label>IGNORE</label>
			<description>IGNORE</description>
			<parent ref="EID28051c99-ff98-480c-b8c1-b502333cc3cd"/>
			<testCases>
				<TestCase id="EID90bfaac5-d8c9-4f50-b609-09ad11af0c27">
					<label>Code lists</label>
					<description>Verify that code lists extensions can be accessed.</description>
					<parent ref="EID4a718a43-3738-4a57-aa9e-a1dbbda89bd9"/>
					<testSteps>
						<TestStep id="EID80717d9e-882c-4ffa-8047-83e9c0d11523">
							<label>IGNORE</label>
							<description>IGNORE</description>
							<parent ref="EID90bfaac5-d8c9-4f50-b609-09ad11af0c27"/>
							<statementForExecution>not applicable</statementForExecution>
							<testItemType ref="EIDf483e8e8-06b9-4900-ab36-adad0d7f22f0"/>
							<testAssertions>
								<TestAssertion id="EIDe6bb652a-5eb3-4c80-ad51-2bccb514d7d8">
									<label>so-ia.a.1: OtherHorizonNotationTypeValue</label>
									<description><![CDATA[Verify that any code list extensions are publicly accessible via HTTP, i.e. inspect extensible code list valued property elements. If a reference (@xlink:href) has a value that does not start with http://inspire.ec.europa.eu/codelist/, verify that a HTTP GET request to the URI retrieves a document. <br/><br/>
Relevant requirements:
<ul><li>IR Requirement Article 6 (3): Code Lists and Enumerations. Where, for an attribute whose type is a code list as referred to in points (b), (c) or (d) of paragraph 1, a data provider provides a value that is not specified in this Regulation, that value and its definition shall be made available in a register.</li></ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/code-list" target="_blank">Abstract Test Case 'Code lists'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.5.1</a>]]></description>
									<parent ref="EID80717d9e-882c-4ffa-8047-83e9c0d11523"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[ self::so:SoilHorizon ]
let $property_string := 'OtherHorizonNotationTypeValue'
let $messages := 

	for $feature in $features_selected/so:otherHorizonNotation/so:OtherHorizonNotationType
		let $envDom := $feature/so:horizonNotation
		let $urls := 
			for $x in $envDom/@xlink:href
				return
				if (starts-with($x, 'http://inspire.ec.europa.eu/codelist/')) then
					()
				else
					$x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
				let $url := string($href)
				let $validuri := map:get($map, $url)
				return
					if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then
						$url
					else ()

	return
		if (not(exists($envDom))) then
			local:addMessage('TR.missingElement',  map {'filename': local:filename($feature/../..), 'featureType':local-name($feature/../..), 'gmlid': string($feature/../../@*:id), 'property': $property_string})
		else if (count($envDom) > 1) then 
			local:addMessage('TR.wrongMultiplicity',  map {'filename': local:filename($feature/../..), 'featureType':local-name($feature/../..), 'gmlid': string($feature/../../@*:id), 'element': $property_string, 'multiplicity': '1', 'count':count($envDom)}) 
		else if (exists($envDom) and not(exists($envDom/@xlink:href))) then
			local:addMessage('TR.missingAttribute',  map {'filename': local:filename($feature/../..), 'featureType':local-name($feature/../..), 'gmlid': string($feature/../../@*:id),'attribute':'@xlink:href', 'property': $property_string})
		else if (exists($envDom) and count($invalid_urls) > 0) then
			local:addMessage('TR.brokenLink', map {'filename': local:filename($feature/../..), 'featureType':local-name($feature/../..), 'gmlid': string($feature/../../@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
		else ()



return 
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)

									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.missingElement"/>
										<translationTemplate ref="TR.wrongMultiplicity"/>
										<translationTemplate ref="TR.missingAttribute"/>
										<translationTemplate ref="TR.brokenLink"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EIDe96c38e2-f605-461b-a081-8bc899b638c2">
									<label>so-ia.a.2: OtherSoilNameTypeValue</label>
									<description><![CDATA[Verify that any code list extensions are publicly accessible via HTTP, i.e. inspect extensible code list valued property elements. If a reference (@xlink:href) has a value that does not start with http://inspire.ec.europa.eu/codelist/, verify that a HTTP GET request to the URI retrieves a document. <br/><br/>
Relevant requirements:
<ul><li>IR Requirement Article 6 (3): Code Lists and Enumerations. Where, for an attribute whose type is a code list as referred to in points (b), (c) or (d) of paragraph 1, a data provider provides a value that is not specified in this Regulation, that value and its definition shall be made available in a register.</li></ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/code-list" target="_blank">Abstract Test Case 'Code lists'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.5.1</a>]]></description>
									<parent ref="EID80717d9e-882c-4ffa-8047-83e9c0d11523"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[ self::so:ObservedSoilProfile | self::so:DerivedSoilProfile ]
let $property_string := 'OtherSoilNameTypeValue'
let $messages := 

	for $feature in $features_selected/so:otherSoilName/so:OtherSoilNameType
		let $envDom := $feature/so:soilName
		let $urls := 
			for $x in $envDom/@xlink:href
				return
				if (starts-with($x, 'http://inspire.ec.europa.eu/codelist/')) then
					()
				else
					$x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
				let $url := string($href)
				let $validuri := map:get($map, $url)
				return
					if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then
						$url
					else ()

	return
		if (not(exists($envDom))) then
			local:addMessage('TR.missingElement',  map {'filename': local:filename($feature/../..), 'featureType':local-name($feature/../..), 'gmlid': string($feature/../../@*:id), 'property': $property_string})
		else if (count($envDom) > 1) then 
			local:addMessage('TR.wrongMultiplicity',  map {'filename': local:filename($feature/../..), 'featureType':local-name($feature/../..), 'gmlid': string($feature/../../@*:id), 'element': $property_string, 'multiplicity': '1', 'count':count($envDom)}) 
		else if (exists($envDom) and not(exists($envDom/@xlink:href))) then
			local:addMessage('TR.missingAttribute',  map {'filename': local:filename($feature/../..), 'featureType':local-name($feature/../..), 'gmlid': string($feature/../../@*:id),'attribute':'@xlink:href', 'property': $property_string})
		else if (exists($envDom) and count($invalid_urls) > 0) then
			local:addMessage('TR.brokenLink', map {'filename': local:filename($feature/../..), 'featureType':local-name($feature/../..), 'gmlid': string($feature/../../@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
		else ()



return 
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)

									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.missingElement"/>
										<translationTemplate ref="TR.wrongMultiplicity"/>
										<translationTemplate ref="TR.missingAttribute"/>
										<translationTemplate ref="TR.brokenLink"/>
									</translationTemplates>
								</TestAssertion>
							</testAssertions>
						</TestStep>
					</testSteps>
				</TestCase>
				<TestCase id="EID6cb80752-1f3a-4955-a50b-eaf514ef01a9">
					<label>Feature references</label>
					<description>Verify that referenced features can be retrieved.</description>
					<parent ref="EID4a718a43-3738-4a57-aa9e-a1dbbda89bd9"/>
					<testSteps>
						<TestStep id="EID4363223e-2f3a-4632-bbf1-f4dee7245b49">
							<label>IGNORE</label>
							<description>IGNORE</description>
							<parent ref="EID6cb80752-1f3a-4955-a50b-eaf514ef01a9"/>
							<statementForExecution>not applicable</statementForExecution>
							<testItemType ref="EIDf483e8e8-06b9-4900-ab36-adad0d7f22f0"/>
							<testAssertions>
								<TestAssertion id="EID75c73399-09f8-4d53-ae32-058cdefd4960">
									<label>so-ia.b.1: DerivedSoilProfile.isDerivedFrom</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:DerivedSoilProfile]
let $gml_ids := $features//@gml:id
let $property_string := 'DerivedSoilProfile.isDerivedFrom'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:isDerivedFrom
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EID8acb2ed5-76d0-4722-bcab-dde4e20ea587">
									<label>so-ia.b.2: ObservedSoilProfile.location</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:ObservedSoilProfile]
let $gml_ids := $features//@gml:id
let $property_string := 'ObservedSoilProfile.location'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:location
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (not(exists($envDom))) then
				local:addMessage('TR.missingElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string})
			else if (count($envDom) > 1) then
				local:addMessage('TR.wrongMultiplicity',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'element': $property_string, 'multiplicity': 1, 'count': count($envDom)}) 
			else if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.missingElement"/>
										<translationTemplate ref="TR.wrongMultiplicity"/>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EIDd8ecbb07-94d1-43f1-92f0-d8ae08b02edf">
									<label>so-ia.b.3: ProfileElement.isPartOf</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:SoilHorizon|self::so:SoilLayer]
let $gml_ids := $features//@gml:id
let $property_string := 'ProfileElement.isPartOf'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:isPartOf
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (not(exists($envDom))) then
				local:addMessage('TR.missingElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string})
			else if (count($envDom) > 1) then
				local:addMessage('TR.wrongMultiplicity',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'element': $property_string, 'multiplicity': 1, 'count': count($envDom)}) 
			else if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.missingElement"/>
										<translationTemplate ref="TR.wrongMultiplicity"/>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EID1c41bb29-4e2f-4b74-99d7-0a8562b099d2">
									<label>so-ia.b.4: ProfileElement.profileElementObservation</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:SoilHorizon|self::so:SoilLayer]
let $gml_ids := $features//@gml:id
let $property_string := 'ProfileElement.profileElementObservation'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:profileElementObservation
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EID5268e34e-4505-473d-891b-337f38f8eb85">
									<label>so-ia.b.5: SoilBody.isDescribedBy</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:SoilBody]
let $gml_ids := $features//@gml:id
let $property_string := 'SoilBody.isDescribedBy'
let $messages_idb :=
	for $feature in $features_selected
		let $envDom := $feature/so:isDescribedBy
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (not(exists($envDom))) then
				local:addMessage('TR.missingElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string})
			else if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()

let $messages_idbp :=
	for $feature in $features_selected/so:isDescribedBy/so:DerivedProfilePresenceInSoilBody
		let $envDom := $feature/so:isDescribedBy
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (not(exists($envDom))) then
				local:addMessage('TR.missingElement',  map {'filename': local:filename($feature/../..), 'featureType':local-name($feature/../..), 'gmlid': string($feature/../../@*:id), 'property': $property_string})
			else if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature/../..), 'featureType':local-name($feature/../..), 'gmlid': string($feature/../../@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature/../..), 'featureType':local-name($feature/../..), 'gmlid': string($feature/../../@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()

let $messages := ($messages_idb, $messages_idbp)


return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.missingElement"/>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EID2ce8dd67-35a4-484f-ab73-743723d724fa">
									<label>so-ia.b.6: SoilDerivedObject.isBasedOnSoilDerivedObject</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:SoilDerivedObject]
let $gml_ids := $features//@gml:id
let $property_string := 'SoilDerivedObject.isBasedOnSoilDerivedObject'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:isBasedOnSoilDerivedObject
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EID2351bde4-a6dc-4e89-acf8-d74cd5cd63cf">
									<label>so-ia.b.7: SoilDerivedObject.isBasedOnObservedSoilProfile</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:SoilDerivedObject]
let $gml_ids := $features//@gml:id
let $property_string := 'SoilDerivedObject.isBasedOnObservedSoilProfile'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:isBasedOnObservedSoilProfile
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EID288dcb02-f27f-4e67-8ac7-ebcdbc2c741e">
									<label>so-ia.b.8: SoilDerivedObject.isBasedOnSoilBody</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:SoilDerivedObject]
let $gml_ids := $features//@gml:id
let $property_string := 'SoilDerivedObject.isBasedOnSoilBody'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:isBasedOnSoilBody
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EID0ce2c904-39b5-45f0-bda2-4b21f50f668c">
									<label>so-ia.b.9: SoilDerivedObject.soilDerivedObjectObservation</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:SoilDerivedObject]
let $gml_ids := $features//@gml:id
let $property_string := 'SoilDerivedObject.soilDerivedObjectObservation'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:soilDerivedObjectObservation
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (not(exists($envDom))) then
				local:addMessage('TR.missingElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string})
			else if (count($envDom) > 1) then
				local:addMessage('TR.wrongMultiplicity',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'element': $property_string, 'multiplicity': 1, 'count': count($envDom)}) 
			else if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>										
										<translationTemplate ref="TR.missingElement"/>
										<translationTemplate ref="TR.wrongMultiplicity"/>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EID8c0590d0-cd28-4dcf-8ee4-12a6ffad2770">
									<label>so-ia.b.10: SoilPlot.locatedOn</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:SoilPlot]
let $gml_ids := $features//@gml:id
let $property_string := 'SoilPlot.locatedOn'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:locatedOn
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (count($envDom) > 1) then
				local:addMessage('TR.wrongMultiplicity',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'element': $property_string, 'multiplicity': 1, 'count': count($envDom)}) 
			else if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.wrongMultiplicity"/>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EIDbc106a36-f58b-43e7-87d9-9d2cf36edd55">
									<label>so-ia.b.11: SoilPlot.observedProfile</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:SoilPlot]
let $gml_ids := $features//@gml:id
let $property_string := 'SoilPlot.observedProfile'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:observedProfile
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (not(exists($envDom))) then
				local:addMessage('TR.missingElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string})
			else if (count($envDom) > 1) then
				local:addMessage('TR.wrongMultiplicity',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'element': $property_string, 'multiplicity': 1, 'count': count($envDom)}) 
			else if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.missingElement"/>
										<translationTemplate ref="TR.wrongMultiplicity"/>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EID8b94e7dc-7783-4dd5-a6f7-30551ddb44b5">
									<label>so-ia.b.12: SoilProfile.isDescribedBy</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:ObservedSoilProfile|self::so:DerivedSoilProfile]
let $gml_ids := $features//@gml:id
let $property_string := 'SoilProfile.isDescribedBy'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:isDescribedBy
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (not(exists($envDom))) then
				local:addMessage('TR.missingElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string})
			else if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.missingElement"/>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EID46f74093-8c7b-4810-aaf0-0f1d836a6664">
									<label>so-ia.b.13: SoilProfile.soilProfileObservation</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:ObservedSoilProfile|self::so:DerivedSoilProfile]
let $gml_ids := $features//@gml:id
let $property_string := 'SoilProfile.soilProfileObservation'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:soilProfileObservation
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.missingElement"/>
										<translationTemplate ref="TR.wrongMultiplicity"/>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EID5e7d43e9-c2e8-44a8-b6db-6e105473150f">
									<label>so-ia.b.14: SoilSite.isObservedOnLocation</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:SoilSite]
let $gml_ids := $features//@gml:id
let $property_string := 'SoilSite.isObservedOnLocation'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:isObservedOnLocation
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (not(exists($envDom))) then
				local:addMessage('TR.missingElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string})
			else if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.missingElement"/>
										<translationTemplate ref="TR.wrongMultiplicity"/>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EID09314227-ebf1-44af-8def-1bf4fe52b06c">
									<label>so-ia.b.15: SoilSite.soilSiteObservation</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:SoilSite]
let $gml_ids := $features//@gml:id
let $property_string := 'SoilSite.soilSiteObservation'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:soilSiteObservation
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EID261a0f07-a1a4-439f-bbf3-238b75c1a014">
									<label>so-ia.b.16: SoilThemeCoverage.isDescribedBy</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:SoilThemeCoverage]
let $gml_ids := $features//@gml:id
let $property_string := 'SoilThemeCoverage.isDescribedBy'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:isDescribedBy
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
								<TestAssertion id="EID23f854a1-78c7-4f5b-a310-766b58696bcf">
									<label>so-ia.b.17 SoilThemeDescriptiveCoverage.isDescribing</label>
									<description><![CDATA[Verify that any feature reference in an association role is publicly accessible via HTTP, i.e. inspect all relevant properties. If a reference (@xlink:href) has a value that starts with "#", verify that an element with a `gml:id` attribute with the referenced identifier exists in the same document. If the reference starts with "http", verify that a HTTP GET request to the URI retrieves an XML document.<br/><br/>
Relevant requirements:
<ul>
<li>IR Requirement Article 4 (2): Types for the Exchange and Classification of Spatial Objects. Spatial object types and data types shall comply with the definitions and constraints and include the attributes and association roles set out in the Annexes.</li>
</ul>
Source: <a href="http://inspire.ec.europa.eu/id/ats/data-so/master/so-ia/features" target="_blank">Abstract Test Case 'Feature references'</a>, <a href="http://inspire.ec.europa.eu/documents/Data_Specifications/INSPIRE_DataSpecification_SO_v3.0.pdf">INSPIRE Data Specification on Soil, A.1.4</a>]]></description>
									<parent ref="EID4363223e-2f3a-4632-bbf1-f4dee7245b49"/>
									<expectedResult>NOT_APPLICABLE</expectedResult>
									<expression>
let $features_selected := $features[self::so:SoilThemeDescriptiveCoverage]
let $gml_ids := $features//@gml:id
let $property_string := 'SoilThemeDescriptiveCoverage.isDescribing'
let $messages :=
	for $feature in $features_selected
		let $envDom := $feature/so:isDescribing
		let $urls := 
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then ()
				else $x
		let $map := local:check-resource-uris($urls, 30, true())
		let $invalid_urls :=
			for $href in $urls
			let $url := string($href)
			let $validuri := map:get($map, $url)
			return
				if ($validuri = 'notHTTP' or starts-with($validuri, 'EXCEPTION') or $validuri = 'idNotFound' or matches($validuri,'^\d{3}$')) then $url
				else ()

		let $invalid_element_reference :=
			for $x in $envDom/@xlink:href
			return
				if (starts-with($x, '#')) then
					if (not(substring($x, 2) = $gml_ids)) then
						$x
					else ()
				else ()
		return
			if (not(exists($envDom))) then
				local:addMessage('TR.missingElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string})
			else if (count($envDom) > 1) then
				local:addMessage('TR.wrongMultiplicity',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'element': $property_string, 'multiplicity': 1, 'count': count($envDom)}) 
			else if (count($invalid_urls) > 0) then
				local:addMessage('TR.brokenLink',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_urls, ',')}) 
			else if (count($invalid_element_reference) > 0) then
				local:addMessage('TR.missingReferencedElement',  map {'filename': local:filename($feature), 'featureType':local-name($feature), 'gmlid': string($feature/@*:id), 'property': $property_string, 'value': string-join($invalid_element_reference, ',')}) 
			else ()
return
(if ($messages) then 'FAILED' else 'PASSED',
local:error-statistics('TR.recordsWithErrors', count(fn:distinct-values($messages//etf:argument[@token='id']/text()))),
$messages)									
									</expression>
									<testItemType ref="EIDf0edc596-49d2-48d6-a1a1-1ac581dcde0a"/>
									<translationTemplates>
										<translationTemplate ref="TR.missingElement"/>
										<translationTemplate ref="TR.wrongMultiplicity"/>
										<translationTemplate ref="TR.brokenLink"/>
										<translationTemplate ref="TR.missingReferencedElement"/>
									</translationTemplates>
								</TestAssertion>
							</testAssertions>
						</TestStep>
					</testSteps>
				</TestCase>
			</testCases>
		</TestModule>
	</testModules>
</ExecutableTestSuite>